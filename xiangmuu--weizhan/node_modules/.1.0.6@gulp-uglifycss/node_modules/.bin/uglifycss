#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.0.0.25@uglifycss/uglifycss" "$@"
  ret=$?
else 
  node  "$basedir/../../../.0.0.25@uglifycss/uglifycss" "$@"
  ret=$?
fi
exit $ret
